---
title: "Differential Abundance"
---

```{r}
#| include: false
setup_file <- if (file.exists("_report_setup.R")) "_report_setup.R" else file.path("report", "code", "_report_setup.R")
source(setup_file)
da_results <- sort_by_q(read_if_exists(file.path(plan$paths$results_dir, "differential_abundance_results.tsv")))
da_status <- read_if_exists(file.path(plan$paths$results_dir, "differential_abundance_model_status.tsv"))
```

## Results

```{r}
table_out(da_results, "Differential abundance results sorted by q-value", n = 100)
```

## Model Status

```{r}
table_out(da_status, "Differential abundance model status", n = 100)
```
