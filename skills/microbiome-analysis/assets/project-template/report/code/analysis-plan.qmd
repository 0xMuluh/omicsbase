---
title: "Analysis Plan"
---

```{r}
#| include: false
setup_file <- if (file.exists("_report_setup.R")) "_report_setup.R" else file.path("report", "code", "_report_setup.R")
source(setup_file)
model_spec <- read_if_exists("config/model_specification.csv")
decision_log <- read_if_exists("config/decision_log.tsv")
```

## Paths

```{r}
table_out(kv_table(plan$paths), "Configured paths")
```

## Identifiers

```{r}
table_out(kv_table(plan$identifiers), "Identifier mapping")
```

## Preprocessing

```{r}
table_out(kv_table(plan$preprocessing), "Preprocessing decisions")
```

## Features

```{r}
table_out(kv_table(plan$features), "Feature handling decisions")
```

## Variables

```{r}
table_out(kv_table(plan$variables), "Analysis variables")
```

## Model Specification

```{r}
table_out(model_spec, "Configured model rows", n = 100)
```

## Decision Log

```{r}
table_out(decision_log, "Recorded decisions", n = 100)
```
