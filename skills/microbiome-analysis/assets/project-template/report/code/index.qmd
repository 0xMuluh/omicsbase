---
title: "ANALYSIS_TITLE"
---

```{r}
#| include: false
setup_file <- if (file.exists("_report_setup.R")) "_report_setup.R" else file.path("report", "code", "_report_setup.R")
source(setup_file)
sample_summary <- read_if_exists(file.path(plan$paths$results_dir, "sample_summary.tsv"))
feature_summary <- read_if_exists(file.path(plan$paths$results_dir, "feature_summary.tsv"))
validation_summary <- read_if_exists(file.path(plan$paths$results_dir, "validation_summary.tsv"))
```

## Study

```{r}
table_out(kv_table(plan$study), "Study configuration")
```

## Run Status

```{r}
table_out(validation_summary, "Required artifact validation")
```

## Sample Summary

```{r}
table_out(sample_summary, "Sample processing summary")
```

## Feature Summary

```{r}
table_out(feature_summary, "Feature filtering summary")
```
