---
title: "Figures"
---

```{r}
#| label: report-context
#| include: false
find_first <- function(paths) {
  hits <- paths[file.exists(paths)]
  if (length(hits)) hits[[1]] else NA_character_
}
load_plan <- function() {
  path <- find_first(c("config/analysis_plan.R", "../config/analysis_plan.R", "../../config/analysis_plan.R"))
  if (is.na(path)) return(NULL)
  env <- new.env(parent = baseenv())
  sys.source(path, envir = env)
  if (exists("analysis_plan", envir = env, inherits = FALSE)) get("analysis_plan", envir = env) else NULL
}
read_optional_table <- function(paths) {
  path <- find_first(paths)
  if (is.na(path)) return(NULL)
  ext <- tolower(tools::file_ext(path))
  if (ext %in% c("tsv", "txt")) read.delim(path, check.names = FALSE, stringsAsFactors = FALSE) else read.csv(path, check.names = FALSE, stringsAsFactors = FALSE)
}
load_result_object <- function() {
  path <- find_first(c(
    "results/metabolomics_results.rds", "../results/metabolomics_results.rds", "../../results/metabolomics_results.rds",
    "output/results/targeted_v2/results.rds", "../output/results/targeted_v2/results.rds", "../../output/results/targeted_v2/results.rds"
  ))
  if (is.na(path)) return(NULL)
  readRDS(path)
}
empty_state <- function(text) cat(text)
plan <- load_plan()
result_obj <- load_result_object()
model_spec <- read_optional_table(c("config/model_specification.csv", "../config/model_specification.csv", "../../config/model_specification.csv"))
model_status <- if (!is.null(result_obj) && "model_status" %in% names(result_obj)) result_obj$model_status else read_optional_table(c("results/model_status.tsv", "../results/model_status.tsv", "../../results/model_status.tsv"))
```


## Analytic sample structure

Generate sample-flow, visit-count, and repeated-measures participation figures when the derived analysis object supports them.

## Complete-case model coverage

Show complete-case sample-size distributions by model family.

## Reported result rows

Show testing burden without mixing incompatible row types.

## Inferential signal summary

Show significant result-row counts and fractions by model family, keeping coefficient families separate when needed.

## Primary-vs-sensitivity stability

Show estimate movement and threshold reclassification under expanded adjustment or other sensitivity settings.

## Representative volcano plots

Use representative or exemplar exposures only with captions that mark them as post hoc unless they were pre-specified.

## Heatmaps or timing comparisons

Use heatmaps, timing ladders, or timing scatter plots to summarize high-dimensional results.

## Longitudinal or repeated-measures status

Keep warnings, singular fits, rank deficiency, and returned-row status visible.
